version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: none
  enable:
    - govet
    - unused
    - errcheck
    - staticcheck
  settings:
    staticcheck:
      checks:
        - all
        - -QF1001  # De Morgan's law - disabled: original is clearer in test assertions
